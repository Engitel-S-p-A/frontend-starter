import { ScxModal } from './scx-modal';

describe('scx-modal', () => {
  it('is defined', () => {
    expect(ScxModal).toBeDefined();
  });

  it('has tag name', () => {
    expect(ScxModal.name).toBe('ScxModal');
  });
});
