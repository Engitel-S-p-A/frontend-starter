import { ScxScorePanel } from './scx-score-panel';

describe('scx-score-panel', () => {
  it('is defined', () => {
    expect(ScxScorePanel).toBeDefined();
  });

  it('has tag name', () => {
    expect(ScxScorePanel.name).toBe('ScxScorePanel');
  });
});
