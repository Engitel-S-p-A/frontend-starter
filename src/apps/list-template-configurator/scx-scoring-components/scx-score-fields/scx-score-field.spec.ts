import { ScxScoreField } from './scx-score-field';

describe('scx-score-field', () => {
  it('is defined', () => {
    expect(ScxScoreField).toBeDefined();
  });

  it('has tag name', () => {
    expect(ScxScoreField.name).toBe('ScxScoreField');
  });
});
