import { ScxAddScore } from './scx-add-score';

describe('scx-add-score', () => {
  it('is defined', () => {
    expect(ScxAddScore).toBeDefined();
  });

  it('has tag name', () => {
    expect(ScxAddScore.name).toBe('ScxAddScore');
  });
});
