import { ScxFileUploader } from './scx-file-uploader';

describe('scx-file-uploader', () => {
  it('is defined', () => {
    expect(ScxFileUploader).toBeDefined();
  });

  it('has tag name', () => {
    expect(ScxFileUploader.name).toBe('ScxFileUploader');
  });
});
