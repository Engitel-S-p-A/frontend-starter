import { ScxMappingTable } from './scx-mapping-table';

describe('scx-mapping-table', () => {
  it('is defined', () => {
    expect(ScxMappingTable).toBeDefined();
  });

  it('has tag name', () => {
    expect(ScxMappingTable.name).toBe('ScxMappingTable');
  });
});
