import { ScxAddModel } from './scx-add-model';

describe('scx-add-model', () => {
  it('is defined', () => {
    expect(ScxAddModel).toBeDefined();
  });

  it('has tag name', () => {
    expect(ScxAddModel.name).toBe('ScxAddModel');
  });
});
